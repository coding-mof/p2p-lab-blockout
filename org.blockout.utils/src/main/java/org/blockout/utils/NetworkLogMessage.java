package org.blockout.utils;

public class NetworkLogMessage {

    public static NetworkLogMessage parse( String line ) {
        // TODO: parse line for message
        return new NetworkLogMessage();
    }
}
